Adicionado ao package.json o script para criação de componentes via cli do shadcn, funcionando assim:
``` bash
  npm run addComponent -- nomeComponente 
```
* Lembre-se de substituir "nomeComponente" pelo nome do componente desejado (palavra toda minúscula), [vide documentação](https://ui.shadcn.com/docs/components).

